<%# Render the new comment into the view %>
$("<%= escape_javascript(render @comment) %>").appendTo("#thread");
<%# Replace the submitted form with a new blank one %>
$("#new_comment").replaceWith("<%= escape_javascript(render 'comments/form') %>")
