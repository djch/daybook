<p>
  <em><%= comment.commenter %> commented:</em>
  <span class="fr silver"><%= time_ago_in_words(comment.created_at) %> ago</span>
</p>
<p>
  <%= comment.body %>
</p>
<p>
  <%= link_to 'Delete comment', [comment.article, comment],
              remote: true,
              method: :delete,
              data: { confirm: "Definitely delete #{comment.commenter}\'s comment?" } %>
</p>
