<header class="mv2 tc">
  <div>← <%= link_to 'Read a different article', articles_path %></div>
  <div class="mv2">
    <%= link_to 'Edit this article', edit_article_path(@article) %>
    •
    <%= link_to 'Delete this article', article_path(@article),
        remote: true,
        method: :delete,
          data: { confirm: 'Definitely delete this article?' } %>
  </div>
</header>

<div class="mw7 center mt4 pa3 pa5-ns bg-white br2">
  <h1 class="mt0 f3 f2-ns f1-l"><%= @article.title %></h1>
  <div class="mb4 gray">
    Published on <%= local_date(@article.created_at) %>
  </div>
  <div class="trix-content mb5">
  	<%# renders richly-formatted article text %>
  	<%= @article.text.html_safe %>
  </div>
</div>
<div class="mw7 center mb4">
  <h2><%= pluralize(@article.comments.count, 'comment') %></h2>
  <div id="thread">
    <%= render @article.comments %>
  </div>
  <div>
    <%= render 'comments/form' %>
  </div>
</div>
